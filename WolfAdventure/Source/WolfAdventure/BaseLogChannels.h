#pragma once   // it makes sure that we never include a header file more than once so that the things that we define in this file are not called again and redefined

#include "CoreMinimal.h"
#include "Logging/LogMacros.h"

DECLARE_LOG_CATEGORY_EXTERN(LogBase, Log, All);